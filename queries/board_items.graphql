query BoardDetail($board_id:Int, $limit:Int, $newest_first:Boolean, $page:Int) {
  boards(ids:[$board_id]) {
    items(limit:$limit,newest_first:$newest_first,page:$page) {
        name
        group {
            title
        }
    }
  }
}